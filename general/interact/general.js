/* Gérer les elements de jeu (score, verifier si les cartes sont egales
   si oui ajouter un et les faire disparaitre, sinon les retourner à nouveau) */
let score = 0;

/* Pop-up avec rappel des règles puis donne le choix entre un mode challenge (temps limité, à rebours) et normal */
if (/* mode challenge choisi -bouton correspondant cliqué-) */) {
    modeChallenge();
} else {
    modeNormal();
};

/* Fonction timer 1 */
function modeChallenge() {
    if (/* timer arrive à la fin avant que le joueur est fini */) {
        /* Arrêter le jeu et mettre un message plus option pour retourner à l'accueil -au clic ou au bout de 10s- */
    };
};

/* Fonction timer 2 */
function modeNormal() {
    /* Jeu classique, avec un timer qui compte juste le temps pris */
};

/* Importer les autres fichiers js !!!! (et voir si on peut appeler des fonctions d'autres fichiers) */